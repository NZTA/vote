{
  "name": "vote",
  "version": "1.1.0",
  "license": "BSD-3-Clause",
  "description": "Provides the ability to vote on a Page OR a Comment.",
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.23.0",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "build": "esbuild js/vote.js --minify --outfile=js/vote.min.js",
    "watch": "esbuild js/vote.js --minify --outfile=js/vote.min.js --watch",
    "format": "prettier -w js/vote.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NZTA/vote.git"
  }
}
